# Media Explorer 🎬

Приложение для поиска и просмотра информации о фильмах и сериалах на Android с использованием The Movie Database (TMDB) API.

## Возможности

✅ **Просмотр популярных фильмов** - Главный экран с сеткой популярных фильмов и бесконечной прокруткой  
✅ **Поиск фильмов** - Поиск по названию с выводом результатов в реальном времени  
✅ **Избранное** - Сохранение любимых фильмов в локальной базе данных (Room)  
✅ **Детальная информация** - Просмотр постера, синопсиса, рейтинга и актёрского состава  
✅ **Share Intent** - Поделиться фильмом через социальные сети или мессенджеры  
✅ **Трейлеры** - Открытие информации о фильме в браузере  

## Технический стек

- **Язык**: Java
- **API клиент**: Retrofit 2
- **Сетевое взаимодействие**: OkHttp 3
- **JSON парсинг**: Gson
- **Изображения**: Glide
- **БД**: Room Persistence Library
- **Архитектура**: MVVM (ViewModel, LiveData, Repository)
- **UI**: RecyclerView, CardView, BottomNavigationView
- **Навигация**: Android Navigation Component

## Установка и запуск

### 1. Получение API ключа TMDB

1. Зарегистрируйтесь на https://www.themoviedb.org/settings/account
2. Перейдите в Settings → API
3. Скопируйте **API Key (v3)**

### 2. Добавление ключа в проект

Откройте файл `local.properties` в корне проекта и добавьте:

```properties
TMDB_API_KEY=api_ключ_здесь
```

### 3. Синхронизация проекта

- В Android Studio: **File → Sync Now**

### 4. Запуск приложения

- Используйте эмулятор или подключённое устройство
- Нажмите **Run 'app'** или используйте Shift+F10

## Структура проекта

```
com.example.mediaexplorer/
├── data/
│   ├── remote/
│   │   ├── ApiService.java         # Retrofit interface с API методами
│   │   ├── ApiClient.java          # Конфигурация Retrofit и OkHttp
│   │   ├── ApiKeyInterceptor.java  # Добавление API ключа к запросам
│   │   └── dto/                    # DTO классы для парсинга JSON
│   │       ├── MovieDTO.java
│   │       ├── MovieResponse.java
│   │       ├── CastDTO.java
│   │       └── CreditsResponse.java
│   └── local/
│       ├── AppDatabase.java        # Room Database
│       └── MediaDao.java           # Data Access Object
├── model/
│   ├── MediaItem.java              # Entity для Room
│   └── Cast.java                   # Модель актёра
├── repository/
│   ├── MediaRepository.java        # Interface
│   └── MediaRepositoryImpl.java     # Реализация с сетевыми запросами
├── viewmodel/
│   ├── MainViewModel.java          # ViewModel для экрана популярных фильмов
│   ├── SearchViewModel.java        # ViewModel для поиска
│   ├── FavoritesViewModel.java     # ViewModel для избранного
│   └── DetailsViewModel.java       # ViewModel для деталей фильма
├── ui/
│   ├── adapters/
│   │   ├── MediaAdapter.java       # Адаптер для списка фильмов
│   │   └── CastAdapter.java        # Адаптер для списка актёров
│   ├── main/
│   │   └── MainFragment.java       # Экран популярных фильмов
│   ├── search/
│   │   └── SearchFragment.java     # Экран поиска
│   ├── favorites/
│   │   └── FavoritesFragment.java  # Экран избранного
│   └── details/
│       └── DetailsFragment.java    # Экран деталей фильма
├── MainActivity.java               # Главная Activity с NavController
├── SplashActivity.java             # Заставка (Splash Screen)
└── util/
    └── Resource.java               # Класс для управления состояниями
```

## Экраны приложения

### 1. **Splash Screen** 
   - Логотип приложения и название
   - Отображается 2 секунды при запуске

### 2. **Popular** (Main)
   - Сетка из 2 колонок с популярными фильмами
   - Бесконечная прокрутка - автозагрузка следующей страницы
   - Кликабельные элементы и кнопка добавления в избранное

### 3. **Search**
   - Поле ввода для поиска
   - Кнопка поиска
   - Сетка с результатами поиска
   - Поддержка пагинации

### 4. **Favorites**
   - Список сохранённых фильмов из Room БД
   - Возможность удаления из избранного
   - "Пусто" сообщение если нет сохранённых фильмов

### 5. **Details**
   - Большой постер фильма
   - Название, синопсис, рейтинг, дата выхода
   - Горизонтальный список актёрского состава
   - Кнопки:
     - "Add to Favorites" - добавить в избранное
     - "Share" - поделиться через Intent.ACTION_SEND
     - "Trailer" - открыть страницу на TMDB в браузере

## Archитектура MVVM

Приложение использует MVVM архитектуру для разделения логики и UI:

- **Model**: MediaItem, Cast (Room entities и DTO)
- **View**: Fragments и Activities (UI слой)
- **ViewModel**: Содержит бизнес-логику, управляет состоянием через LiveData
- **Repository**: Абстрактный слой между ViewModel и Data источниками

### Поток данных:
```
API/Room DB → Repository → ViewModel → LiveData → Fragment/Activity (UI)
```

## Как это работает

### Загрузка популярных фильмов:
1. Fragment вызывает `viewModel.loadPopular(page)`
2. ViewModel обращается к Repository
3. Repository делает сетевой запрос через Retrofit к TMDB API
4. ApiKeyInterceptor добавляет API ключ к запросу
5. Ответ парсится в DTO и преобразуется в MediaItem
6. Результат передаётся в LiveData
7. Fragment наблюдает LiveData и обновляет UI

### Сохранение в избранное:
1. Fragment вызывает `viewModel.addToFavorites(item)`
2. ViewModel обращается к Repository
3. Repository вставляет MediaItem в Room БД в фоновом потоке
4. LiveData с избранными фильмами автоматически обновляется

## Зависимости

- AndroidX (AppCompat, Fragment, Navigation)
- Retrofit 2 и OkHttp 3 для сетевых запросов
- Glide для загрузки и кэширования изображений
- Room для локального хранилища
- Lifecycle (ViewModel, LiveData)
- RecyclerView и Material Components

Все версии указаны в `gradle/libs.versions.toml`


